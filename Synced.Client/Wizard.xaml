<Window x:Class="Synced.Client.Wizard"  
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"  
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"  
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"  
        xmlns:local="clr-namespace:Synced.Client"  
        xmlns:res="clr-namespace:Synced.Client.Windows.Resources"  
        mc:Ignorable="d"  
         Height="450" Width="300" Title="{x:Static res:AppLocalization.WizardTitle}">  
    <Grid>  
        <Grid.RowDefinitions>  
            <RowDefinition  Height="*"/>  
            <RowDefinition Height="Auto"/>  
        </Grid.RowDefinitions>  
        <Frame x:Name="WizardFrame" NavigationUIVisibility="Visible" Margin="10" ScrollViewer.CanContentScroll="True" ScrollViewer.VerticalScrollBarVisibility="Auto"></Frame>  
        <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Right">  
            <StackPanel.Resources>  
                <Style TargetType="{x:Type Button}">  
                    <Setter Property="Width" Value="50"/>  
                    <Setter Property="Height" Value="25"/>  
                    <Setter Property="Margin" Value="5"/>  
                </Style>  
            </StackPanel.Resources>  
            <Button x:Name="WizardBack" Content="{x:Static res:AppLocalization.BackStep}" Click="WizardBack_Click"></Button>  
            <Button x:Name="WizardNext" Content="{x:Static res:AppLocalization.NextStep}" Click="WizardNext_Click"></Button>  
        </StackPanel>  
    </Grid>  
</Window>
